import { createApp } from 'vue'
import App from './App.vue'
import './style.css'

// Font Awesome — CSS only, no JS component wrapper
import '@fortawesome/fontawesome-free/css/all.min.css'

createApp(App).mount('#app')